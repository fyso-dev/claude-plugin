# Claude Adapter

This adapter adds Claude-specific runtime artifacts on top of the shared Fyso AI Plugin core.

Included here:

- `.claude-plugin/` manifests and marketplace metadata
- `hooks/` for Claude runtime automation
- `agents/` for Claude agent manifests
- Claude installation documentation
